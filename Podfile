# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'JKDBModel' do
  pod 'JKDBModel', :path => "/Users/cgs/Documents/JKDBModel"
  target 'JKDBModelTests' do
    inherit! :search_paths
  end
end
